<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
    <div class="container">
        <div class="navbar-brand">
            <a href="/"><i class="fas fa-users text-success fa-2x"></i></a>
        </div>
    </div>
</nav>

<div class="container">
        <h1 class="text-center mt-4">Edite os dados</h1>
        <div class="row justify-content-center">
            
            <form action="/atualizar" method="POST" class="col-6 bg-dark mt-2">
                <input type="hidden" name="id" value="{{pessoa._id}}">
                <div class="form-group">
                    <label for="nome" class="text-light">Nome:</label>
                    <input type="text" name="nome" value="{{pessoa.nome}}" class="form-control">
                </div>

                <div class="form-group">
                    <label for="nome" class="text-light">CPF:</label>
                    <input type="text" name="cpf" value="{{pessoa.cpf}}" class="form-control">
                </div>

                <div class="form-group">
                    <label for="nome" class="text-light">Data de Nascimento:</label>
                    <input type="text" name="data_de_nascimento" value="{{moment pessoa.data_de_nascimento format="DD/MM/YYYY"}}" class="form-control">
                </div>
                <button type="submit" class="btn btn-success mt-3 mb-3 form-control">Editar</button>
            </form>
        </div>
    </div>